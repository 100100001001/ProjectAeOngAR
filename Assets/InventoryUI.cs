using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InventoryUI : MonoBehaviour
{
    //public GameObject inventoryPanel; // Inventory UI 오브젝트가 담길 변수

    //bool activeInventory = false; // Inventory UI 오브젝트의 Open 또는 Close 상태를 표현할 bool형 변수

    void Start()
    {
        //// inventoryPanel의 활성화 상태를 activeInventory값(비활성)으로 변경
        //inventoryPanel.SetActive(activeInventory);
    }

    void Update()
    {
        
    }
}
